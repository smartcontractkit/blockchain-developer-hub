import HomepageTestimonial from './HomepageTestimonial';

export default {
  title: 'section/Homepage Testimonial',
  component: HomepageTestimonial,
};

const Template = (args) => <HomepageTestimonial {...args} />;

export const Default = Template.bind({});
