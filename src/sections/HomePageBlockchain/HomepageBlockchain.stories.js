import HomepageBlockchain from './HomepageBlockchain';

export default {
  title: 'section/Homepage Blockchain',
  component: HomepageBlockchain,
};

const Template = (args) => <HomepageBlockchain {...args} />;

export const Default = Template.bind({});
