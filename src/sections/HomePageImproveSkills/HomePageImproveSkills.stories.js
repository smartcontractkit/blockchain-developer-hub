import HomepageImproveSkills from './HomePageImproveSkills';

export default {
  title: 'section/Homepage Improve Skills',
  component: HomepageImproveSkills,
};

const Template = (args) => <HomepageImproveSkills {...args} />;

export const Default = Template.bind({});
