import CaseStudyBanner from './EcosystemBanner';

export default {
  title: 'section/HomePage Banner',
  component: CaseStudyBanner,
};

const Template = (args) => <CaseStudyBanner {...args} />;

export const Default = Template.bind({});
